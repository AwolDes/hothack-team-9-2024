<!DOCTYPE html>
<html>
  <head>
    <title>Hothack</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= hotwire_livereload_tags if Rails.env.development? %>
  </head>

  <body>
    <div class="nav_bar p-4 flex justify-between items-center max-w-screen bg-gray-400 text-white">
      <% if user_signed_in? %>
        <nav class="signed_in_nav w-full">
          <div class="w-full" id="navbar-default">
            <ul class="w-full font-medium flex justify-between items-center rtl:space-x-reverse">
              <li>
                <div class="flex gap-2">
                  <span class="self-center text-2xl font-semibold whitespace-nowrap">Modular</span>
                  <%= image_tag "music_note.png", class: "h-8", alt: "Modular logo" %>
                </div>
              </li>
              <li>
                <%= link_to "Tours", "/tours", class: "block py-2 px-3 rounded hover:bg-gray-100 md:hover:bg-transparent md:border-0 md:hover:text-fuchsia-600 md:p-0" %>
              </li>
              <li>
                <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "block py-2 px-3 rounded hover:bg-gray-100 md:hover:bg-transparent md:border-0 md:hover:text-fuchsia-600 md:p-0" %>
              </li>
            </ul>
          </div>
        </nav>
      <% else %>
        <nav class="signed_out_nav w-full">
          <div class="w-full" id="navbar-default">
            <ul class="w-full font-medium flex justify-between items-center rtl:space-x-reverse">
              <li>
                <div class="flex gap-2">
                  <span class="self-center text-2xl font-semibold whitespace-nowrap">Modular</span>
                  <%= image_tag "music_note.png", class: "h-8", alt: "Modular logo" %>
                </div>
              </li>
              <li>
                <%= button_to "Sign in", new_user_session_path, class: "block py-2 px-3 rounded hover:bg-gray-100 md:hover:bg-transparent md:border-0 md:hover:text-fuchsia-600 md:p-0" %> 
              </li>
            </ul>
          </div>
        </nav>
      <% end %>
    </div>

    <main class="mx-auto max-w-screen bg-gray-100">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>

      <%= yield %>
    </main>
  </body>
</html>
